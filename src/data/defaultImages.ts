// Default images — semua dikosongkan agar data datang dari Spreadsheet / CMS
// Jika tidak ada data di Spreadsheet, gambar tidak akan ditampilkan.

export const DEFAULT_HERO_IMAGE = '';

export const DEFAULT_BANNER_IMAGE = '';

export const DEFAULT_LOGO = '';

export const DEFAULT_BOT_AVATAR = '';

export const DEFAULT_APP_ICON = '';
